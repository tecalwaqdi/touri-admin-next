/**
 * Phase 5B test helper — Customer Controlled Writes implemented in Phase 5C
 * (Production still disabled). Finance writes not started.
 * AGENT_WRITE_ENABLED remains false (implementation exists; Production off).
 */
export const AGENT_WRITE_ENABLED_RUNTIME = false;
/** Implementation exists (Phase 5C); Production CUSTOMER_WRITE_ENABLED remains false. */
export const CUSTOMER_WRITE_IMPLEMENTED = true;
export const FINANCE_WRITE_IMPLEMENTED = false;
